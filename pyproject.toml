[project]
name = "cicd-pipeline-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ihorholinei", email = "ihorholinei@icloud.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.1",
]

[project.scripts]
cicd-pipeline-demo = "cicd_pipeline_demo:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "ruff>=0.16.1",
]
[tool.uv]
package = false